package com.game.engine.strategy;

import com.game.engine.entity.Character;

/**
 * Interface defining the contract for character move strategies.
 * Different strategies will implement different behaviors for character moves.
 * 
 * TODO: Define the method signature for executing a move
 */
public interface MoveStrategy {
    // TODO: Define method signature for move execution
    // Hint: The method should take a Character as parameter (the target)
    // Hint: The method should take the Character performing the move as parameter
}
